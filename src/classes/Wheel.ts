import Cli from "./Cli";
import Vehicle from "./Vehicle"

class Wheel {

    static createWheels(): Wheel[] {

        // Implementation here

        return [new Wheel(), new Wheel(), new Wheel(), new Wheel()]; // Example implementation

    }

}
export default Wheel
